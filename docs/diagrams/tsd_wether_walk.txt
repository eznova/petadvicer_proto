@startuml
actor "Pet Owner" as a1
participant UI as p1
participant Server as p2
participant "Weather API" as p7
participant "Walk Advicer" as p8
participant "DB" as p9

autonumber
a1 -> p1 : Открой личный кабинет
p1 -> p2 : верни_статус_авторизации_пользователя
alt пользователь неавторизован
p2 -> p1 : пользователь_неавторизован
p1 -> a1 : введи_лог_пароль
a1 -> p1 : логин+пароль
p1 -> p2 : верни_данные_о_питомце(логин)
else пользователь авторизован
p1 -> p2 : верни_данные_о_питомце(логин)
end
p2 -> p9 : запрос_данных_о_питомце
p2 <- p9 : данные о питомце
p2 -> p7 : Город пользователя
p2 <- p7 : сводка погоды
p2 -> p8 : температура + скорость ветра + вес питомца
p2 <- p8 : Совет по условиям прогулки с питомцем
p1 <- p2 : Код страницы сайта с данными о погоде и советами
p1 -> a1 : Данные о погоде и рекомендации к прогулке

@enduml
